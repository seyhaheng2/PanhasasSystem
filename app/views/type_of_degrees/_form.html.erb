<%= form_for(@type_of_degree, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @type_of_degree.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@type_of_degree.errors.count, "error") %> prohibited this type_of_degree from being saved:</h4>

      <ul>
      <% @type_of_degree.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-6">
    <div class="inline-form">
      <%= f.label :name, :class => "c-label" %>
      <%= f.text_field :name%>
    </div>
  </div>
<div class="col-md-12">
    <br>
      <%= f.submit :class => "btn btn-primary" %>
      <%= link_to type_of_degrees_path, :class => 'btn btn-default' do %>
    <span class="fa fa-list-alt"></span>
    Back
  <% end %>
</div>
<% end %>
